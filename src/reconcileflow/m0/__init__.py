"""M0 spike: the pre-registered decision rules of Evaluation Run #001.

Decision logic only. The load generator, the ground-truth oracle and the
probes need the real ``target_stack`` (protocol §0) and are deliberately absent.
"""
